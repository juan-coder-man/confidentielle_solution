/** Tarifa base fija en centavos COP (formato confidentielle: centavos). */
export const BASE_FEE_CENTS = 50_000;

/** Costo de envío fijo en centavos COP. */
export const DELIVERY_FEE_CENTS = 80_000;
